
## Auto-distill Task Flow UI + Worktree 隔离（2026-03-28）

1. **做了什么**：将 Progress 面板完全重构为基于 `/api/tasks` 的看板布局；改造数据流：从 sidebar 轮询的 state 改为直接调用 tasks API；更新 CSS 和标签文本。

2. **踩了什么坑**：函数签名变更（renderProgressPanel 去掉 state 参数）需要同步更新所有调用方；轮询控制从 sidebar 转移到标签切换后容易遗漏清理旧的计时器引用。

3. **可复用的模式**：UI 组件替换时，先追踪现有的数据源和轮询逻辑，然后整体迁移到新的 API 接口；通过标签事件控制轮询生命周期比全局轮询更清晰。

4. **遗留问题**：无。API 端点和 UI 布局已就位，后续可直接叠加看板卡片模板。

## Auto-distill Add Multi-PDF Slides Support（2026-03-28）

1. **做了什么**：修复history replay时plan_approval/question卡片按钮错误激活的bug——通过在renderEvent中传递isResolved标志，让历史事件的交互组件自动禁用。

2. **踩了什么坑**：history与live两条渲染路径共用同一个renderPlanApproval函数但状态处理不同，导致已处理的历史卡片仍显示active按钮；用户点击无响应但触发Claude重新进入plan mode导致上下文混乱。

3. **可复用的模式**：对有状态的交互组件（特别是表单/按钮），渲染函数应显式接收"是否为历史/已解决"标志，而非依赖全局状态推断；resolveCard()应作为状态锁定的标准方法。

4. **遗留问题**：需验证两个plan_approval事件（255、277）在修复后是否正确显示为disabled状态；检查是否有其他交互组件类型需要类似处理。
